<section class="user">
  <%= profile_image(@user) %>
  <h1><%= @user.name %></h1>
  <h2><%= mail_to(@user.email) %></h2>
  <h3>
    Member Since:
    <p>
      <%= @user.created_at.strftime("%B %Y") %>
    </p>
  </h3>
</section>
